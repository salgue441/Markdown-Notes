/**
 * Topbar component
 *
 * @details Allows the user to drag the window around
 * @returns {JSX.Element} - The topbar component
 */
export const DraggableTopbar = (): JSX.Element => {
  return <header className="absolute inset-0 h-8 bg-transparent " />
}
